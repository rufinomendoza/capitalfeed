<h2>Sign up—Daily Briefing</h2>

<p class="futura-book">If you want to get a copy of the same daily news briefing we use ourselves, as well as participate in the testing of new products, you can sign up below. If you are looking for our established product—the monthly briefing—you can get it <%= link_to "here", subscribe_path %>.</p>

<%= simple_form_for(resource, :as => resource_name, :url => registration_path(resource_name), html: { class: 'form-horizontal'}) do |f| %>
  <%= f.error_notification %>

  <%= f.input :email %>
  <%= f.input :password %>
  <%= f.input :password_confirmation %>

  <div class="form-actions">
    <%= f.submit "Sign up", class: "btn" %>
  </div>
  <div class="form-actions">
    <%= link_to "Sign in", new_user_session_path, class: "btn" %>
    <%= link_to "Forgot password?", new_user_password_path, class: "btn" %>
  </div>
  <div class="form-actions">
    <%= link_to "Sign in with Google", user_omniauth_authorize_path(:google_oauth2), class: "btn" %>
  </div>
<% end %>